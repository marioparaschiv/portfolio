type Clickables = {
	elements: string[];
	classes: string[];
	ids: string[];
};

export const Clickables: Clickables = {
	elements: ['a', 'button'],
	classes: ['clickable', 'cursor-pointer'],
	ids: []
};

export const Role = 'Full-Stack Developer';